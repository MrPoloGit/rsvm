# Custom System Verilog Modules

Repository for custom modules

### signed_pipelined_multiplier
- handles signed or unsigned
- customizable DataWidth
- customizable pipeline amount
- handles overflow and underflow
- Handles zero inputs, last stage optimized?
- PREOP and POSTOP stages?

### signed_pipelined_divider
- handles signed or unsigned
- customizable DataWidth
- customizable pipeline amount
- customizable divisor = 0 handling, set to 0, set to specific parameter
- handles overflow and underflow
- Handles zero inputs, last stage optimized?
- PREOP and POSTOP stages?